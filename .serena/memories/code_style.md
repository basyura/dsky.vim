Vim script: 2-space indentation; settings/commands at top-level in plugin/, logic in autoload/.
TypeScript: 2-space indentation, semicolons, named exports from feature modules.
Naming: lowercase filenames with underscores or folder grouping (e.g., autoload/dsky/util/str.vim).